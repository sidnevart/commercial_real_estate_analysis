"""
Скрипт для извлечения САМОГО ЛОКАЛЬНОГО элемента адреса (улица/район/город)
Исправленная версия с улучшенным GPT-анализом
"""

import logging
import sys
import re
from pathlib import Path
from typing import List, Dict, Tuple, Optional

sys.path.append(str(Path(__file__).parent))

from core.gpt_tunnel_client import sync_chat

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s: %(message)s")
logger = logging.getLogger(__name__)

def extract_most_local_address_part(address: str) -> str:
    """
    Извлекает САМЫЙ ЛОКАЛЬНЫЙ элемент адреса по приоритету:
    1. Улица (если есть) - самое локальное
    2. Район/микрорайон 
    3. Город
    4. Область
    """
    if not address:
        return "Неизвестно"
        
    address_lower = address.lower().strip()
    logger.debug(f"Извлечение локального элемента: '{address}'")
    
    # 1. ПРИОРИТЕТ: Улицы (самое локальное)
    street_patterns = [
        r'ул\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'улица\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'пр-т\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'проспект\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'пер\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'переулок\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'б-р\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'бульвар\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'наб\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'набережная\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'ш\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'шоссе\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        # Паттерны для формата "Название улица"
        r'([А-Яа-яё\s\-0-9]+?)\s+улица(?:\s*,|\s+д|\s+дом|\s*$)',
        r'([А-Яа-яё\s\-0-9]+?)\s+проспект(?:\s*,|\s+д|\s+дом|\s*$)',
    ]
    
    for pattern in street_patterns:
        match = re.search(pattern, address_lower)
        if match:
            street_name = match.group(1).strip().title()
            # Очищаем от лишних символов
            street_name = re.sub(r'\s+', ' ', street_name)
            if len(street_name) > 3:  # Минимальная длина названия улицы
                logger.info(f"✅ Найдена улица (самое локальное): {street_name}")
                return street_name
    
    # 2. Районы/микрорайоны (если улица не найдена)
    district_patterns = [
        r'мкр\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'микрорайон\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'р-н\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'район\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'([А-Яа-яё\s\-]+?)\s+район(?:\s*,|\s*$)',
        r'([А-Яа-яё\s\-]+?)\s+микрорайон(?:\s*,|\s*$)',
    ]
    
    for pattern in district_patterns:
        match = re.search(pattern, address_lower)
        if match:
            district_name = match.group(1).strip().title()
            if len(district_name) > 2:
                logger.info(f"✅ Найден район: {district_name}")
                return district_name
    
    # 3. Известные конкретные районы Москвы
    moscow_districts = [
        "Перово", "Новогиреево", "Измайлово", "Богородское", "Вешняки",
        "Басманный", "Хамовники", "Арбат", "Тверской", "Пресненский",
        "Останкинский", "Сокольники", "Красносельский", "Таганский",
        "Алексеевский", "Бабушкинский", "Бибирево", "Бутырский",
        "Кузьминки", "Люблино", "Текстильщики", "Марьино", "Братеево",
        "Академический", "Гагаринский", "Черёмушки", "Коньково",
        "Митино", "Строгино", "Крылатское", "Кунцево", "Солнцево",
        "Зюзино", "Котловка", "Дорогомилово", "Можайский", "Очаково-Матвеевское",
        "Замоскворечье", "Якиманка", "Мещанский", "Лефортово", "Некрасовка",
        "Печатники", "Рязанский", "Южнопортовый", "Нижегородский", "Капотня",
        "Даниловский", "Донской", "Зябликово", "Москворечье-Сабурово",
        "Нагатино-Садовники", "Нагатинский Затон", "Нагорный", "Царицыно",
        "Чертаново Северное", "Чертаново Центральное", "Чертаново Южное",
        "Бирюлёво Восточное", "Бирюлёво Западное", "Ломоносовский", "Обручевский",
        "Северное Бутово", "Тёплый Стан", "Южное Бутово", "Ясенево",
        "Ново-Переделкино", "Проспект Вернадского", "Раменки", "Тропарёво-Никулино",
        "Филёвский Парк", "Фили-Давыдково", "Куркино", "Покровское-Стрешнево",
        "Северное Тушино", "Хорошёво-Мнёвники", "Щукино", "Южное Тушино"
    ]
    
    for district in moscow_districts:
        if district.lower() in address_lower:
            logger.info(f"✅ Найден известный район Москвы: {district}")
            return district
    
    # 4. Города МО (без префикса)
    mo_cities = {
        'клин': 'Клин', 'мытищи': 'Мытищи', 'подольск': 'Подольск',
        'орехово-зуево': 'Орехово-Зуево', 'лыткарино': 'Лыткарино',
        'воскресенск': 'Воскресенск', 'химки': 'Химки', 'балашиха': 'Балашиха',
        'королёв': 'Королёв', 'люберцы': 'Люберцы', 'красногорск': 'Красногорск',
        'одинцово': 'Одинцово', 'домодедово': 'Домодедово', 'реутов': 'Реутов',
        'пушкино': 'Пушкино', 'щёлково': 'Щёлково', 'ногинск': 'Ногинск',
        'серпухов': 'Серпухов', 'электросталь': 'Электросталь',
        'жуковский': 'Жуковский', 'коломна': 'Коломна', 'котельники': 'Котельники',
        'дзержинский': 'Дзержинский', 'лобня': 'Лобня', 'ивантеевка': 'Ивантеевка',
        'фрязино': 'Фрязино', 'долгопрудный': 'Долгопрудный', 'видное': 'Видное',
        'климовск': 'Климовск', 'солнечногорск': 'Солнечногорск'
    }
    
    for city_key, city_name in mo_cities.items():
        if city_key in address_lower:
            logger.info(f"✅ Найден город МО: {city_name}")
            return city_name
    
    # 5. Зеленоград
    if 'зеленоград' in address_lower:
        return "Зеленоград"
    
    # 6. Fallback к общим областям
    if any(word in address_lower for word in ['московская область', 'мо']):
        return "Московская область"
    elif any(word in address_lower for word in ['москва']):
        return "Москва"
    
    return "Неизвестно"

def gpt_extract_most_local_part_fixed(address: str) -> str:
    """
    ИСПРАВЛЕННЫЙ GPT-анализ с точными инструкциями
    """
    if not address:
        return "Неизвестно"
        
    prompt = f"""
Проанализируй российский адрес и найди САМЫЙ ЛОКАЛЬНЫЙ элемент.

АДРЕС: "{address}"

ЗАДАЧА: Выбери ТОЛЬКО ОДИН самый конкретный элемент по приоритету:

ПРИОРИТЕТ (от самого важного):
1. 🔥 УЛИЦА - самое важное! (Гагарина, Новогиреевская, Мира, Тверская)
2. 📍 РАЙОН/МИКРОРАЙОН - если нет улицы (Перово, Хамовники, Климовск)  
3. 🏙️ ГОРОД - если нет улицы и района (Клин, Зеленоград, Воскресенск)
4. 🌍 ОБЛАСТЬ - только в крайнем случае

КОНКРЕТНЫЕ ПРИМЕРЫ:

✅ ПРАВИЛЬНО:
"ул Гагарина, дом 6" → ОТВЕТ: Гагарина
"пр-т Мира, 119" → ОТВЕТ: Мира  
"г Москва, ВАО, Перово, ул Новогиреевская" → ОТВЕТ: Новогиреевская
"Москва, район Хамовники" → ОТВЕТ: Хамовники
"мкр Климовск" → ОТВЕТ: Климовск
"Зеленоград, корпус 847" → ОТВЕТ: Зеленоград
"обл Московская, г.о. Воскресенск, рп им. Цюрупы" → ОТВЕТ: им. Цюрупы
"Тверская улица, 7" → ОТВЕТ: Тверская
"Останкинский район" → ОТВЕТ: Останкинский

❌ НЕПРАВИЛЬНО:
"ул Гагарина" → НЕ "агарина" (НЕ срезай первую букву!)
"пр-т Мира" → НЕ "ира" (НЕ срезай начало!)
"Тверская улица" → НЕ "Тверская улица" (убери слово "улица"!)
"Останкинский район" → НЕ "Останкинский район" (убери слово "район"!)

СТРОГИЕ ПРАВИЛА:
- НЕ срезай первые буквы названий!
- НЕ добавляй префиксы (ул., г., мкр.)
- НЕ включай слова "улица", "район", "микрорайон" в ответ
- НЕ выбирай округа (ВАО, СВАО, ЦАО)
- Сохраняй полные названия: "Академика Каргина", "им. Цюрупы"

АЛГОРИТМ:
1. Ищи улицу (ул., пр-т, б-р, наб.) - если есть, выбери её
2. Если улицы нет - ищи район/микрорайон  
3. Если района нет - ищи город
4. Если города нет - область

Верни ТОЛЬКО чистое название без объяснений, префиксов и слов "улица"/"район".
"""
    
    try:
        response = sync_chat(
            model="gpt-4o-mini",
            messages=[
                {
                    "role": "system", 
                    "content": "Ты точный анализатор российских адресов. НИКОГДА не срезай первые буквы названий. ВСЕГДА возвращай полные правильные названия улиц, районов и городов БЕЗ слов 'улица' и 'район'."
                },
                {"role": "user", "content": prompt}
            ],
            max_tokens=100
        )
        
        result = response.strip().strip('"').strip("'").strip()
        
        # Расширенная очистка префиксов и суффиксов
        prefixes_to_remove = [
            r'^ул\.?\s+',
            r'^улица\s+', 
            r'^пр-т\.?\s+',
            r'^проспект\s+',
            r'^наб\.?\s+',
            r'^набережная\s+',
            r'^б-р\.?\s+',
            r'^бульвар\s+',
            r'^пер\.?\s+',
            r'^переулок\s+',
            r'^ш\.?\s+',
            r'^шоссе\s+',
            r'^г\.?\s+',
            r'^город\s+',
            r'^мкр\.?\s+',
            r'^микрорайон\s+',
            r'^район\s+',
            r'^р-н\.?\s+',
            r'^м\.?\s+',
            r'^метро\s+'
        ]
        
        # Суффиксы для удаления
        suffixes_to_remove = [
            r'\s+улица$',
            r'\s+проспект$',
            r'\s+бульвар$',
            r'\s+набережная$',
            r'\s+переулок$',
            r'\s+шоссе$',
            r'\s+район$',
            r'\s+микрорайон$'
        ]
        
        # Применяем очистку префиксов осторожно
        for prefix_pattern in prefixes_to_remove:
            if re.match(prefix_pattern, result, flags=re.IGNORECASE):
                result = re.sub(prefix_pattern, '', result, flags=re.IGNORECASE).strip()
                break  # Удаляем только первый найденный префикс
        
        # Применяем очистку суффиксов
        for suffix_pattern in suffixes_to_remove:
            result = re.sub(suffix_pattern, '', result, flags=re.IGNORECASE).strip()
        
        # Дополнительная проверка на корректность
        if len(result) >= 2 and len(result) <= 80:
            # Проверяем, что не вернулись запрещённые значения
            forbidden = ['москва', 'московская область', 'мо', 'вао', 'свао', 'цао', 'юао', 'сао', 'зао', 'юзао', 'сзао']
            if result.lower() not in forbidden:
                logger.info(f"🎯 GPT ИСПРАВЛЕННЫЙ извлек: '{result}'")
                return result
        
        # Если результат подозрительный, используем fallback
        logger.warning(f"GPT вернул подозрительный результат: '{result}', применяем REGEX fallback")
        return extract_most_local_address_part(address)
            
    except Exception as e:
        logger.error(f"Ошибка исправленного GPT: {e}")
        return extract_most_local_address_part(address)

def gpt_extract_most_local_part_simple(address: str) -> str:
    """
    Простой GPT-анализ (для сравнения)
    """
    if not address:
        return "Неизвестно"
        
    prompt = f"""
Найди САМЫЙ ЛОКАЛЬНЫЙ элемент в адресе:

АДРЕС: "{address}"

ПРИОРИТЕТ: Улица → Район → Город → Область

ПРИМЕРЫ:
- "ул Гагарина, дом 6" → "Гагарина"
- "г Москва, ВАО, Перово, ул Новогиреевская" → "Новогиреевская"
- "район Хамовники" → "Хамовники"
- "г Клин" → "Клин"

Верни ТОЛЬКО название без префиксов.
"""
    
    try:
        response = sync_chat(
            model="gpt-4o-mini",
            messages=[
                {"role": "system", "content": "Найди самый локальный элемент адреса."},
                {"role": "user", "content": prompt}
            ],
            max_tokens=50
        )
        
        result = response.strip().strip('"').strip("'")
        result = re.sub(r'^(ул\.?|г\.?|мкр\.?|район)\s*', '', result, flags=re.IGNORECASE)
        result = result.strip()
        
        if 2 <= len(result) <= 50:
            return result
        else:
            return "Неизвестно"
            
    except Exception as e:
        logger.error(f"Ошибка простого GPT: {e}")
        return "Неизвестно"

def test_fixed_gpt_extraction():
    """Тестирует все три метода: REGEX, GPT простой, GPT исправленный"""
    
    test_cases = [
        # Проблемные случаи, которые нужно исправить
        ("обл Московская, г.о. Клин, г Клин, ул Гагарина, дом 6", "Гагарина"),
        ("г Москва, пр-т Мира, 119", "Мира"),
        ("г Москва, ВАО, Перово", "Перово"),
        ("обл Московская, г.о. Воскресенск, рп им. Цюрупы", "им. Цюрупы"),
        ("Московская область", "Московская область"),
        
        # Остальные тест-кейсы
        ("обл Московская, г.о. Подольск, г Подольск, ул Правды, дом 20", "Правды"),
        ("г Москва, ВАО, Перово, ул Новогиреевская, д 42", "Новогиреевская"),
        ("Московская область, Мытищи, ул. Академика Каргина, 38к1", "Академика Каргина"),
        ("Москва, м. Новогиреево, ул. Новогиреевская, 28", "Новогиреевская"),
        ("Москва, Тверская улица, 7", "Тверская"),
        ("г Москва, ул Тверская, дом 7", "Тверская"),
        ("Москва, Пресненская набережная, дом 12", "Пресненская"),
        ("г Москва муниципальный округ Басманный ул Покровка дом 42", "Покровка"),
        ("Московская область, г Химки, ул Загородная, дом 4", "Загородная"),
        
        # Проспекты, бульвары
        ("Москва, Ленинский проспект, БЦ Горизонт", "Ленинский"),
        ("Москва, б-р Чистопрудный, 12", "Чистопрудный"),
        
        # Случаи без улиц - районы
        ("Москва, район Хамовники", "Хамовники"),
        ("г. Москва, СВАО, Останкинский район", "Останкинский"),
        ("мкр Климовск", "Климовск"),
        
        # Города (если нет улиц и районов)
        ("Зеленоград, корпус 847", "Зеленоград"),
        ("Российская Федерация, Московская область, городской округ Балашиха", "Балашиха"),
        
        # Сложные случаи
        ("г Москва муниципальный округ Басманный", "Басманный"),
        ("Москва, м. Арбатская, Арбат, 10", "Арбат"),  # Арбат - и улица и район
        
        # Граничные случаи
        ("", "Неизвестно"),
    ]
    
    print("🧪 ТЕСТИРОВАНИЕ ИСПРАВЛЕННОГО GPT: REGEX vs GPT-ПРОСТОЙ vs GPT-ИСПРАВЛЕННЫЙ")
    print("=" * 110)
    print(f"{'№':<3} {'Метод':<15} {'Результат':<12} {'Адрес':<50} {'Ожидаемый':<15} {'Получен'}")
    print("=" * 110)
    
    correct_regex = 0
    correct_gpt_simple = 0
    correct_gpt_fixed = 0
    total = len(test_cases)
    
    for i, (address, expected) in enumerate(test_cases, 1):
        # Тестируем все три метода
        regex_result = extract_most_local_address_part(address)
        gpt_simple_result = gpt_extract_most_local_part_simple(address)
        gpt_fixed_result = gpt_extract_most_local_part_fixed(address)
        
        # Проверяем правильность
        regex_correct = regex_result == expected
        gpt_simple_correct = gpt_simple_result == expected
        gpt_fixed_correct = gpt_fixed_result == expected
        
        if regex_correct:
            correct_regex += 1
        if gpt_simple_correct:
            correct_gpt_simple += 1
        if gpt_fixed_correct:
            correct_gpt_fixed += 1
        
        # Форматируем вывод
        address_short = address[:45] + "..." if len(address) > 45 else address
        
        regex_status = "✅ OK" if regex_correct else "❌ FAIL"
        gpt_simple_status = "✅ OK" if gpt_simple_correct else "❌ FAIL"
        gpt_fixed_status = "✅ OK" if gpt_fixed_correct else "❌ FAIL"
        
        print(f"{i:<3} {'REGEX':<15} {regex_status:<12} {address_short:<50} {expected:<15} {regex_result}")
        print(f"{i:<3} {'GPT-ПРОСТОЙ':<15} {gpt_simple_status:<12} {address_short:<50} {expected:<15} {gpt_simple_result}")
        print(f"{i:<3} {'GPT-ИСПРАВЛЕННЫЙ':<15} {gpt_fixed_status:<12} {address_short:<50} {expected:<15} {gpt_fixed_result}")
        print("-" * 110)
    
    print("=" * 110)
    print(f"📊 ДЕТАЛЬНОЕ СРАВНЕНИЕ РЕЗУЛЬТАТОВ:")
    print(f"REGEX:             {correct_regex}/{total} ({correct_regex/total*100:.1f}%) успешно")
    print(f"GPT-ПРОСТОЙ:       {correct_gpt_simple}/{total} ({correct_gpt_simple/total*100:.1f}%) успешно")
    print(f"GPT-ИСПРАВЛЕННЫЙ:  {correct_gpt_fixed}/{total} ({correct_gpt_fixed/total*100:.1f}%) успешно")
    
    # Определяем лучший метод
    scores = {
        "regex": correct_regex,
        "gpt_simple": correct_gpt_simple,
        "gpt_fixed": correct_gpt_fixed
    }
    
    best_method = max(scores, key=scores.get)
    best_score = scores[best_method]
    
    print(f"\n🏆 ЛУЧШИЙ МЕТОД: {best_method.upper().replace('_', '-')} с результатом {best_score}/{total}")
    
    if best_method == "gpt_fixed":
        print("🎉 ИСПРАВЛЕННЫЙ GPT показал лучший результат!")
        print("💡 Рекомендуется использовать gpt_extract_most_local_part_fixed")
    elif best_method == "gpt_simple":
        print("🎯 ПРОСТОЙ GPT показал лучший результат!")
        print("💡 Рекомендуется использовать gpt_extract_most_local_part_simple")
    else:
        print("📊 REGEX показал лучший результат!")
        print("💡 Рекомендуется использовать extract_most_local_address_part")
    
    return best_method

# Для обратной совместимости
def improved_lot_district_parser(address: str) -> str:
    """Обратная совместимость - использует исправленный GPT"""
    return gpt_extract_most_local_part_fixed(address)

def gpt_parse_lot_district(address: str) -> str:
    """Обратная совместимость - использует исправленный GPT"""
    return gpt_extract_most_local_part_fixed(address)

"""if __name__ == "__main__":
    print("🎯 ТЕСТИРОВАНИЕ ИСПРАВЛЕННОГО GPT")
    print("Исправляем проблемы: срезание букв, неточные результаты")
    print("Приоритет: Улица → Район → Город → Область")
    print()
    
    best_method = test_fixed_gpt_extraction()
    
    print(f"\n💡 ИСПРАВЛЕНИЯ В GPT:")
    print("✅ Строгие инструкции НЕ срезать первые буквы")
    print("✅ Осторожная очистка только явных префиксов")
    print("✅ Удаление слов 'улица' и 'район' из результата")
    print("✅ Проверка на запрещённые значения")
    print("✅ Fallback к REGEX при подозрительных результатах")
    print("✅ Увеличенный лимит токенов для точности")"""