"""
Тестовый скрипт для отладки парсинга районов объявлений ЦИАН
Исправленная версия с улучшенным GPT-анализом
"""

import logging
import sys
import re
from pathlib import Path
from typing import List, Dict, Tuple

sys.path.append(str(Path(__file__).parent))

from core.gpt_tunnel_client import sync_chat

logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s: %(message)s")
logger = logging.getLogger(__name__)

def extract_most_local_cian_part(address: str) -> str:
    """
    Извлекает САМЫЙ ЛОКАЛЬНЫЙ элемент адреса ЦИАН по приоритету:
    1. Улица (если есть) - самое локальное
    2. Район/микрорайон 
    3. Город
    4. Область
    """
    if not address:
        return "Неизвестно"
        
    address_lower = address.lower().strip()
    logger.debug(f"Извлечение локального элемента ЦИАН: '{address}'")
    
    # 1. ПРИОРИТЕТ: Улицы (самое локальное)
    street_patterns = [
        r'ул\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'улица\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'пр-т\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'проспект\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'пер\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'переулок\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'б-р\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'бульвар\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'наб\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'набережная\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'ш\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        r'шоссе\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s+д|\s+дом|\s*$)',
        # Специальные паттерны для ЦИАН - формат "Название улица"
        r'([А-Яа-яё\s\-0-9]+?)\s+улица(?:\s*,|\s+д|\s+дом|\s*$)',  # "Тверская улица"
        r'([А-Яа-яё\s\-0-9]+?)\s+проспект(?:\s*,|\s+д|\s+дом|\s*$)',  # "Ленинский проспект"
    ]
    
    for pattern in street_patterns:
        match = re.search(pattern, address_lower)
        if match:
            street_name = match.group(1).strip().title()
            # Очищаем от лишних символов
            street_name = re.sub(r'\s+', ' ', street_name)
            if len(street_name) > 3:  # Минимальная длина названия улицы
                logger.info(f"✅ Найдена улица ЦИАН (самое локальное): {street_name}")
                return street_name
    
    # 2. Конкретные районы по станциям метро (характерно для ЦИАН)
    metro_to_district = {
        'новогиреево': 'Новогиреево',
        'перово': 'Перово',
        'измайловская': 'Измайлово',
        'семеновская': 'Соколиная Гора',
        'электрозаводская': 'Соколиная Гора',
        'бабушкинская': 'Бабушкинский',
        'медведково': 'Северное Медведково',
        'бибирево': 'Бибирево',
        'алтуфьево': 'Алтуфьевский',
        'кузьминки': 'Кузьминки',
        'люблино': 'Люблино',
        'текстильщики': 'Текстильщики',
        'арбатская': 'Арбат',
        'тверская': 'Тверской',
        'митино': 'Митино',
        'строгино': 'Строгино',
        'крылатское': 'Крылатское',
        'кунцевская': 'Кунцево',
        'молодежная': 'Можайский',
        'планерная': 'Северное Тушино',
        'щукинская': 'Щукино',
        'тушинская': 'Северное Тушино',
        'сокольники': 'Сокольники',
        'красносельская': 'Красносельский',
        'комсомольская': 'Красносельский'
    }
    
    # Ищем станции метро
    for metro, district in metro_to_district.items():
        if f'м. {metro}' in address_lower or f'метро {metro}' in address_lower:
            logger.info(f"✅ Найден район ЦИАН по метро: {district}")
            return district
    
    # 3. Районы/микрорайоны
    district_patterns = [
        r'мкр\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'микрорайон\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'р-н\.?\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'район\s+([А-Яа-яё\s\-0-9]+?)(?:\s*,|\s*$)',
        r'([А-Яа-яё\s\-]+?)\s+район(?:\s*,|\s*$)',
    ]
    
    for pattern in district_patterns:
        match = re.search(pattern, address_lower)
        if match:
            district_name = match.group(1).strip().title()
            if len(district_name) > 2:
                logger.info(f"✅ Найден район ЦИАН: {district_name}")
                return district_name
    
    # 4. Известные районы Москвы (прямой поиск)
    moscow_districts = [
        "Перово", "Новогиреево", "Измайлово", "Богородское", "Вешняки",
        "Басманный", "Хамовники", "Арбат", "Тверской", "Пресненский",
        "Останкинский", "Сокольники", "Красносельский", "Таганский",
        "Алексеевский", "Бабушкинский", "Бибирево", "Бутырский",
        "Кузьминки", "Люблино", "Текстильщики", "Марьино", "Братеево",
        "Академический", "Гагаринский", "Черёмушки", "Коньково",
        "Митино", "Строгино", "Крылатское", "Кунцево", "Солнцево",
        "Зюзино", "Котловка", "Дорогомилово", "Можайский", "Очаково-Матвеевское",
        "Замоскворечье", "Якиманка", "Мещанский", "Лефортово", "Некрасовка",
        "Печатники", "Рязанский", "Южнопортовый", "Нижегородский", "Капотня"
    ]
    
    for district in moscow_districts:
        if district.lower() in address_lower:
            logger.info(f"✅ Найден известный район Москвы ЦИАН: {district}")
            return district
    
    # 5. Города МО (без префикса)
    mo_cities = {
        'клин': 'Клин', 'мытищи': 'Мытищи', 'подольск': 'Подольск',
        'химки': 'Химки', 'балашиха': 'Балашиха', 'королёв': 'Королёв',
        'люберцы': 'Люберцы', 'красногорск': 'Красногорск', 'одинцово': 'Одинцово',
        'домодедово': 'Домодедово', 'реутов': 'Реутов', 'пушкино': 'Пушкино',
        'щёлково': 'Щёлково', 'ногинск': 'Ногинск', 'орехово-зуево': 'Орехово-Зуево',
        'лыткарино': 'Лыткарино', 'воскресенск': 'Воскресенск'
    }
    
    for city_key, city_name in mo_cities.items():
        if city_key in address_lower:
            logger.info(f"✅ Найден город МО ЦИАН: {city_name}")
            return city_name
    
    # 6. Зеленоград
    if 'зеленоград' in address_lower:
        return "Зеленоград"
    
    # 7. Fallback
    if 'московская область' in address_lower or ', мо' in address_lower:
        return "Московская область"
    elif 'москва' in address_lower:
        return "Москва"
    
    return "Неизвестно"

def gpt_extract_most_local_cian_part_fixed(address: str) -> str:
    """
    ИСПРАВЛЕННЫЙ GPT-анализ для ЦИАН с точными инструкциями
    """
    if not address:
        return "Неизвестно"
        
    prompt = f"""
Проанализируй адрес ЦИАН и найди САМЫЙ ЛОКАЛЬНЫЙ элемент.

АДРЕС ЦИАН: "{address}"

СПЕЦИФИКА ЦИАН:
- Станции метро: "м. Новогиреево" 
- Формат "название улица": "Тверская улица"
- ТЦ и БЦ: "ТЦ Мегаполис", "БЦ Горизонт"

ЗАДАЧА: Выбери ТОЛЬКО ОДИН самый конкретный элемент:

ПРИОРИТЕТ:
1. 🔥 УЛИЦА - самое важное! (Гагарина, Новогиреевская, Академика Каргина)
2. 📍 РАЙОН ПО МЕТРО - если нет улицы (Новогиреево, Кузьминки, Арбат)
3. 📍 РАЙОН - если нет улицы и метро (Перово, Хамовники, Останкинский)
4. 🏙️ ГОРОД - если нет предыдущих (Клин, Мытищи, Зеленоград)

КОНКРЕТНЫЕ ПРИМЕРЫ ЦИАН:

✅ ПРАВИЛЬНО:
"Московская область, Клин, ул. Гагарина, 37/1" → ОТВЕТ: Гагарина
"Москва, м. Новогиреево, ул. Новогиреевская, 28" → ОТВЕТ: Новогиреевская
"Мытищи, ул. Академика Каргина" → ОТВЕТ: Академика Каргина
"Москва, м. Кузьминки, ТЦ Мегаполис" → ОТВЕТ: Кузьминки
"Москва, ВАО, Перово" → ОТВЕТ: Перово
"Зеленоград, корпус 847" → ОТВЕТ: Зеленоград
"Тверская улица, 7" → ОТВЕТ: Тверская
"Останкинский район" → ОТВЕТ: Останкинский

❌ НЕПРАВИЛЬНО:
"ул. Гагарина" → НЕ "агарина" (НЕ срезай!)
"м. Новогиреево" → НЕ "овогиреево" (НЕ срезай!)
"Тверская улица" → НЕ "Тверская улица" (убери слово "улица"!)
"Останкинский район" → НЕ "Останкинский район" (убери слово "район"!)

СТРОГИЕ ПРАВИЛА:
- НЕ срезай первые буквы!
- НЕ добавляй префиксы
- НЕ включай слова "улица", "район", "микрорайон" в ответ
- НЕ выбирай округа (ВАО, СВАО)
- Сохраняй полные названия

АЛГОРИТМ:
1. Ищи улицу - если есть, выбери её
2. Если улицы нет - ищи станцию метро
3. Если метро нет - ищи район
4. Если района нет - ищи город

Верни ТОЛЬКО чистое название без префиксов, объяснений и слов "улица"/"район".
"""
    
    try:
        response = sync_chat(
            model="gpt-4o-mini",
            messages=[
                {
                    "role": "system", 
                    "content": "Ты точный анализатор адресов ЦИАН. НИКОГДА не срезай первые буквы. ВСЕГДА возвращай полные названия улиц, районов и городов БЕЗ слов 'улица' и 'район'."
                },
                {"role": "user", "content": prompt}
            ],
            max_tokens=100
        )
        
        result = response.strip().strip('"').strip("'").strip()
        
        # Расширенная очистка префиксов и суффиксов
        prefixes_to_remove = [
            r'^ул\.?\s+',
            r'^улица\s+', 
            r'^пр-т\.?\s+',
            r'^проспект\s+',
            r'^наб\.?\s+',
            r'^б-р\.?\s+',
            r'^г\.?\s+',
            r'^мкр\.?\s+',
            r'^район\s+',
            r'^м\.?\s+',
            r'^метро\s+'
        ]
        
        # Суффиксы для удаления
        suffixes_to_remove = [
            r'\s+улица$',
            r'\s+проспект$',
            r'\s+бульвар$',
            r'\s+набережная$',
            r'\s+переулок$',
            r'\s+шоссе$',
            r'\s+район$',
            r'\s+микрорайон$'
        ]
        
        # Применяем очистку префиксов осторожно
        for prefix_pattern in prefixes_to_remove:
            if re.match(prefix_pattern, result, flags=re.IGNORECASE):
                result = re.sub(prefix_pattern, '', result, flags=re.IGNORECASE).strip()
                break
        
        # Применяем очистку суффиксов
        for suffix_pattern in suffixes_to_remove:
            result = re.sub(suffix_pattern, '', result, flags=re.IGNORECASE).strip()
        
        # Проверка на корректность
        if len(result) >= 2 and len(result) <= 80:
            forbidden = ['москва', 'московская область', 'мо', 'вао', 'свао', 'цао', 'юао', 'сао', 'зао']
            if result.lower() not in forbidden:
                logger.info(f"🎯 GPT ЦИАН ИСПРАВЛЕННЫЙ извлек: '{result}'")
                return result
        
        logger.warning(f"GPT ЦИАН вернул подозрительный результат: '{result}', применяем REGEX fallback")
        return extract_most_local_cian_part(address)
            
    except Exception as e:
        logger.error(f"Ошибка исправленного GPT ЦИАН: {e}")
        return extract_most_local_cian_part(address)

def gpt_extract_most_local_cian_part_simple(address: str) -> str:
    """
    Простой GPT-анализ для ЦИАН (для сравнения)
    """
    if not address:
        return "Неизвестно"
        
    prompt = f"""
Найди САМЫЙ ЛОКАЛЬНЫЙ элемент в адресе ЦИАН:

АДРЕС: "{address}"

ПРИОРИТЕТ: Улица → Район по метро → Район → Город

ПРИМЕРЫ ЦИАН:
- "ул. Гагарина, 37/1" → "Гагарина"
- "м. Новогиреево, ул. Новогиреевская" → "Новогиреевская"
- "м. Кузьминки, ТЦ Мегаполис" → "Кузьминки"

Верни название без префиксов.
"""
    
    try:
        response = sync_chat(
            model="gpt-4o-mini",
            messages=[
                {"role": "system", "content": "Найди самый локальный элемент адреса ЦИАН."},
                {"role": "user", "content": prompt}
            ],
            max_tokens=50
        )
        
        result = response.strip().strip('"').strip("'")
        result = re.sub(r'^(ул\.?|м\.?|г\.?|мкр\.?)\s*', '', result, flags=re.IGNORECASE)
        result = result.strip()
        
        if 2 <= len(result) <= 50:
            return result
        else:
            return "Неизвестно"
            
    except Exception as e:
        logger.error(f"Ошибка простого GPT ЦИАН: {e}")
        return "Неизвестно"

def test_fixed_cian_extraction():
    """Тестирует все три метода для ЦИАН: REGEX, GPT простой, GPT исправленный"""
    
    test_cases = [
        # Проблемные случаи ЦИАН
        ("Московская область, Клин, ул. Гагарина, 37/1", "Гагарина"),
        ("Московская область, Клин, ул. Гагарина, 49", "Гагарина"),
        ("Московская область, Мытищи, ул. Академика Каргина, 38к1", "Академика Каргина"),
        ("г Москва, пр-т Мира, 119с536", "Мира"),
        ("Москва, ВАО, Перово", "Перово"),
        
        # Остальные тест-кейсы ЦИАН
        ("Московская область, г Химки, ул Загородная, дом 4", "Загородная"),
        ("г Москва, ВАО, Перово, ул Новогиреевская, д 42", "Новогиреевская"),
        ("Москва, м. Новогиреево, ул. Новогиреевская, 28", "Новогиреевская"),
        ("Москва, Тверская улица, 7", "Тверская"),
        ("Москва, м. Арбатская, Арбат, 10", "Арбат"),
        ("Московская область, Подольск, ул. Правды", "Правды"),
        ("Мытищи, ул. Академика Каргина", "Академика Каргина"),
        
        # Проспекты, специфичные для ЦИАН
        ("Москва, Ленинский проспект, БЦ Горизонт", "Ленинский"),
        ("Москва, б-р Чистопрудный, дом 12К5", "Чистопрудный"),
        
        # Районы по метро (характерно для ЦИАН)
        ("Москва, м. Кузьминки, ТЦ Мегаполис", "Кузьминки"),
        ("Москва, м. Новогиреево", "Новогиреево"),
        ("Москва, м. Строгино", "Строгино"),
        
        # Районы без улиц
        ("Зеленоград, корпус 847", "Зеленоград"),
        ("г. Москва, СВАО, Останкинский район", "Останкинский"),
        
        # Сложные случаи ЦИАН
        ("МО, г Подольск, мкр Климовск, ул Заводская", "Заводская"),
        ("Москва г, Чистопрудный бульвар, дом 12К5", "Чистопрудный"),
        
        # Граничные случаи
        ("", "Неизвестно"),
        ("Москва, адрес не указан", "Москва"),
    ]
    
    print("🧪 ТЕСТИРОВАНИЕ ИСПРАВЛЕННОГО GPT ЦИАН: REGEX vs GPT-ПРОСТОЙ vs GPT-ИСПРАВЛЕННЫЙ")
    print("=" * 120)
    print(f"{'№':<3} {'Метод':<18} {'Результат':<12} {'Адрес':<50} {'Ожидаемый':<15} {'Получен'}")
    print("=" * 120)
    
    correct_regex = 0
    correct_gpt_simple = 0
    correct_gpt_fixed = 0
    total = len(test_cases)
    
    for i, (address, expected) in enumerate(test_cases, 1):
        # Тестируем все три метода
        regex_result = extract_most_local_cian_part(address)
        gpt_simple_result = gpt_extract_most_local_cian_part_simple(address)
        gpt_fixed_result = gpt_extract_most_local_cian_part_fixed(address)
        
        # Проверяем правильность
        regex_correct = regex_result == expected
        gpt_simple_correct = gpt_simple_result == expected
        gpt_fixed_correct = gpt_fixed_result == expected
        
        if regex_correct:
            correct_regex += 1
        if gpt_simple_correct:
            correct_gpt_simple += 1
        if gpt_fixed_correct:
            correct_gpt_fixed += 1
        
        # Форматируем вывод
        address_short = address[:45] + "..." if len(address) > 45 else address
        
        regex_status = "✅ OK" if regex_correct else "❌ FAIL"
        gpt_simple_status = "✅ OK" if gpt_simple_correct else "❌ FAIL"
        gpt_fixed_status = "✅ OK" if gpt_fixed_correct else "❌ FAIL"
        
        print(f"{i:<3} {'REGEX':<18} {regex_status:<12} {address_short:<50} {expected:<15} {regex_result}")
        print(f"{i:<3} {'GPT-ПРОСТОЙ':<18} {gpt_simple_status:<12} {address_short:<50} {expected:<15} {gpt_simple_result}")
        print(f"{i:<3} {'GPT-ИСПРАВЛЕННЫЙ':<18} {gpt_fixed_status:<12} {address_short:<50} {expected:<15} {gpt_fixed_result}")
        print("-" * 120)
    
    print("=" * 120)
    print(f"📊 ДЕТАЛЬНОЕ СРАВНЕНИЕ РЕЗУЛЬТАТОВ ЦИАН:")
    print(f"REGEX:               {correct_regex}/{total} ({correct_regex/total*100:.1f}%) успешно")
    print(f"GPT-ПРОСТОЙ:         {correct_gpt_simple}/{total} ({correct_gpt_simple/total*100:.1f}%) успешно")
    print(f"GPT-ИСПРАВЛЕННЫЙ:    {correct_gpt_fixed}/{total} ({correct_gpt_fixed/total*100:.1f}%) успешно")
    
    # Определяем лучший метод
    scores = {
        "regex": correct_regex,
        "gpt_simple": correct_gpt_simple,
        "gpt_fixed": correct_gpt_fixed
    }
    
    best_method = max(scores, key=scores.get)
    best_score = scores[best_method]
    
    print(f"\n🏆 ЛУЧШИЙ МЕТОД ДЛЯ ЦИАН: {best_method.upper().replace('_', '-')} с результатом {best_score}/{total}")
    
    return best_method

# Для обратной совместимости - старые функции с новой логикой
def improved_cian_district_parser(address: str) -> str:
    """Обратная совместимость - использует исправленный GPT для ЦИАН"""
    return gpt_extract_most_local_cian_part_fixed(address)

def gpt_parse_cian_district(address: str) -> str:
    """Обратная совместимость - использует исправленную GPT логику для ЦИАН"""
    return gpt_extract_most_local_cian_part_fixed(address)

"""if __name__ == "__main__":
    print("🎯 ТЕСТИРОВАНИЕ ИСПРАВЛЕННОГО GPT ДЛЯ ЦИАН")
    print("Исправляем проблемы: срезание букв, неточные результаты")
    print("Приоритет: Улица → Район по метро → Район → Город → Область")
    print()
    
    best_method = test_fixed_cian_extraction()
    
    print(f"\n💡 ИСПРАВЛЕНИЯ В GPT ЦИАН:")
    print("✅ Строгие инструкции НЕ срезать буквы")
    print("✅ Учёт специфики ЦИАН: метро, ТЦ, БЦ") 
    print("✅ Осторожная очистка префиксов")
    print("✅ Удаление слов 'улица' и 'район' из результата")
    print("✅ Проверка запрещённых результатов")
    print("✅ Fallback к REGEX при ошибках")"""